/** @type {import('next').NextConfig} */
// import path from "path";

const nextConfig = {
    reactStrictMode: true,
    // webpack: (config) => {
    //     config.resolve.alias["config"] = path.resolve(__dirname, "config/default.json");
    //     return config;
    // },
};

export default nextConfig;
